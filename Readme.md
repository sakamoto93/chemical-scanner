# Chemical Scanner

試薬瓶のラベルから CAS 番号を OCR で読み取り、化合物情報を取得し、一覧化・Excel 出力を行う Web アプリケーション。

---

# プロジェクトの目的

研究室で使用している試薬管理を効率化することを目的とする。

最終目標は既存の試薬管理台帳と統合し、

- CAS番号
- 化合物名
- 法令情報
- SDS
- 在庫管理

などを一元管理できるシステムを構築する。

ただし、最初から全機能を作るのではなく、段階的に開発する。

---

# 開発方針

## 小さく作る

一度に完成形を目指さない。

毎回

「動くもの」

を作る。

---

## GitHubで履歴管理

GitHubをバックアップとしてではなく、

「開発履歴」

として利用する。

動作確認できた段階で必ずコミットする。

iPhone ClaudからもGitHubへの連携ができるようになり、モバイルデバイスからも開発進捗の管理が可能になった。

---

## 常に動く状態を維持

main ブランチは必ず動作する状態にする。

新しい機能は feature ブランチで開発する。

例

main

├── feature/camera

├── feature/ocr

├── feature/pubchem

---

## 一段階ごとに完成させる

各Stepで

- 動作確認
- Git Commit
- GitHubへPush

を行う。

---

# 使用技術

Backend

- FastAPI

Frontend

- HTML
- CSS
- JavaScript

OCR

- PaddleOCR

画像処理

- OpenCV

化学データ

- PubChem API

Excel

- openpyxl

データベース

- Step1では使用しない
- Step3でSQLite導入予定

---

# ディレクトリ構成

chemical_scanner/

```
app.py

requirements.txt

README.md

scanner/

templates/

static/

exports/
```

---

# 開発ロードマップ

## Step1

CAS番号読み取りアプリ

目的

試薬瓶をカメラに向けるだけで

CAS番号

↓

化合物名取得

↓

一覧追加

↓

Excel出力

までを実現する。

---

### Step1-1

FastAPI

HTML

カメラ表示

完成条件

ブラウザにライブ映像が表示されること。

---

### Step1-2

PaddleOCR

完成条件

画面内の文字列が取得できること。

**実装済み** ✅

#### Step1-2.5

自動OCR実行機能（8月11日完成 → 8月11日改善）

**実装済み** ✅

**目的**

ボタンをクリックするだけで、カメラ映像から自動的にテキストを連続抽出できる自動スキャン機能

**実装内容（Phase 1 + Phase 2 + Phase 3）**

**Phase 1: 基本的な定期実行**
- JavaScriptで定期実行（最初は1秒、その後2.5秒に最適化）
- 「自動スキャン: 開始/停止」ボタンで制御
- ステータス表示（停止中/実行中）

**Phase 2: フレーム比較による重複検出回避**
- 前フレームと現在のフレームを比較
- テキストが異なる場合のみ表示を更新
- 不要な画面更新を削減

**Phase 3: 起動遅延とカウントダウン表示（8月11日実装）**
- ユーザーが手動で「自動スキャン: 開始」ボタンをクリック
- 5秒のカウントダウン表示（5→4→3→2→1秒）
- カウントダウン中はスキャンを実行しない
- 5秒後に「実行中...」と表示されてスキャン開始
- ユーザーが試薬瓶を正しい位置に配置する時間を確保

**Phase 4: 設定値のユーザー調整機能（8月11日実装）**
- ブラウザ上で以下の値をユーザーが調整可能：
  - **スタートアップ遅延**: 1〜60秒（デフォルト: 5秒）
  - **スキャン間隔**: 0.5〜30秒（デフォルト: 2.5秒）
- 設定値変更時に即座に変数が更新される
- 次回スキャン開始時に新しい設定値で動作
- 実験環境や試薬の種類に応じて最適な値を調整可能

**スキャン間隔**
- 2.5秒ごとに定期実行（初回最適化で1秒→2.5秒に変更）
- CPU負荷とスキャン精度のバランスを取得

**完成条件** ✅ 達成

- ✅ ボタンをクリックすると、5秒のカウントダウン後に自動スキャン開始
- ✅ テキストが検出されたら画面に表示されること
- ✅ CPU負荷が許容範囲であること
- ✅ 前フレームとの重複比較で不要な更新が削減されること
- ✅ 停止ボタンで即座にスキャンが停止すること

**特徴**

- ボタン1クリックで自動スキャン開始
- カウントダウンでユーザーに準備時間を提供
- 連続スキャンで複数試薬に対応可能
- 前フレーム比較で画面ちらつき防止
- オン/オフで制御可能

**将来の最適化オプション**

- フレーム内に試薬瓶が来たら自動スキャン開始（枠内検出機能）
- OCR処理速度の向上
- フレームレートの動的調整
- WebSocket による高速化

---

### Step1-3

CAS番号抽出・化合物情報取得（8月17日実装開始）

#### Phase 1: CAS番号がある試薬の対応（8月17日完成） ✅

**実装内容**
- 正規表現でOCRテキストからCAS番号を抽出
- PubChem APIで化合物情報を取得（名前、分子式、分子量）
- 抽出されたCAS番号と化合物情報をブラウザに表示

**使用技術**
- `pubchempy` - PubChem APIラッパーライブラリ
- 正規表現パターン: `\d{2,7}-\d{2}-\d`

**実装関数**
- `extract_cas_number()` - 正規表現でCAS番号抽出
- `search_pubchem_by_cas()` - PubChem APIで化合物情報取得

**完成条件** ✅ 達成
- ✅ CAS番号を正しく抽出できること
- ✅ PubChem APIで化合物情報が取得できること
- ✅ 化合物名、分子式、分子量をブラウザに表示できること

**テスト結果**
- テスト試薬: Thymol (CAS: 5142-23-4)
- ✅ CAS番号抽出成功
- ✅ 化合物情報取得成功
- ✅ ブラウザに表示成功

#### Phase 2: CAS番号がない試薬の対応（8月17日完成） ✅

**実装内容**
- OCRで抽出された高信頼度テキストから化合物名を抽出
- PubChem APIで化合物名を検索
- CAS番号を自動取得（IUPAC名、同義語、詳細情報から抽出）
- CAS番号がない場合でも化合物情報を表示

**使用技術**
- `pubchempy.get_compounds()` - 化合物名で検索
- 正規表現でCAS番号パターンを抽出
- 信頼度でテキストをソート（≥85%のみ検索対象）

**実装関数**
- `search_pubchem_by_name()` - 化合物名でPubChem検索

**完成条件** ✅ 達成
- ✅ 化合物名から逆引き検索できること
- ✅ CAS番号を自動取得できること
- ✅ 化合物名、分子式、分子量をブラウザに表示できること
- ✅ 「検出方法」を区別して表示できること（CAS番号/化合物名）

**テスト結果**
- テスト試薬: Thymol（日本語: チモール、別名: 2-Isopropyl-5-methylphenol）
- ✅ 化合物名「Thymol」から検索成功
- ✅ CAS番号: 89-83-8 を自動取得
- ✅ ブラウザに「検出方法: 化合物名から取得」と表示
- ✅ ブラウザに化合物情報を表示成功

---

### Step1-4

PubChem検索

完成条件

CAS番号から化合物名を取得できること。

---

### Step1-5

一覧表示

**実装済み** ✅ (Phase 1: 基本的なリスト表示機能)

**目的**

複数の試薬を連続的に読み取り、CAS番号と化合物名をテーブル形式で一覧管理する。

**Phase 1: 基本的なリスト表示機能（実装完了）**

**実装内容**
- OCR結果に「リストに追加」ボタンを表示（化合物情報検出時のみ）
- テーブル形式でリスト表示（CAS番号、化合物名、分子式、分子量、削除ボタン）
- セッション中にリストを保持（メモリ内管理）
- 各行に削除ボタンを配置

**使用技術**
- JavaScriptの配列 (`compoundList`) でリスト管理
- DOM操作でテーブル行を動的に生成
- イベント処理で追加・削除機能を実装

**実装関数**
- `addToList()` - 現在の化合物情報をリストに追加
- `updateCompoundTable()` - テーブルを動的に更新
- `deleteFromList(index)` - リストから項目を削除

**完成条件** ✅ 達成
- ✅ 化合物情報検出時に「リストに追加」ボタンを表示できること
- ✅ ボタンクリックでリストに追加できること
- ✅ テーブル形式でリストを表示できること
- ✅ 削除ボタンでリストから削除できること
- ✅ 複数の試薬を追加できること
- ✅ 通称名（日本語名など）も表示できること
- ✅ 初回読み取り開始時に正常に動作すること
- ✅ リスト追加後のスムーズなスキャン継続

**改善内容**（iPhone テスト後の調整）
- ✅ PubChemのsynonymsから通称名を自動抽出
- ✅ 日本語名を優先的に表示
- ⚠️ カウントダウン終了後のperformOCR実行（まだ未解決）
- ⚠️ リスト追加後のOCR状態をクリア（連続スキャンは可能だが速度低下）

---

## Step1-5 改善実装状況（8月18日） ✅

### 実装完了した改善

**1. 初回読み取り開始問題 - 修正済み** ✅

改善内容
- ✅ setTimeoutの100ms遅延を削除 → 即座実行に変更
- ✅ 詳細なコンソールログを追加（各処理ステップを記録）
- ✅ エラーハンドリングを強化（例外とネットワークエラーを捕捉）
- ✅ performOCR実行前後のフラグ状態をログ出力

デバッグ方法
- ブラウザ開発者ツール (F12) を開く
- Console タブで以下のログを確認:
  ```
  [Countdown Complete] isRunning: true shouldStop: false
  [Starting OCR] About to call performOCR...
  [performOCR] Starting capture request
  [performOCR] Capture response received in X.XXms
  [performOCR] OCR response received in X.XXms
  ```

テスト状況：実装完了、ローカル/iPhone でのテスト待ち

---

**2. 連続スキャン時のパフォーマンス低下 - 改善済み** ✅

改善内容
- ✅ テーブル差分更新の実装（全行削除から新規行のみ追加に変更）
  - 初回追加時: 全行作成
  - 追加実行時: 新しい行のみを append
  - 削除実行時: インデックス正確性を保証するため全行再構築
- ✅ スキャン間隔を 2.5秒 → 3.5秒に変更（処理頻度を削減）
- ✅ 削除ボタンのインデックス正確性を修正

期待効果
- DOM操作の削減によりレイアウト再計算が軽減
- 連続スキャン中のカメラ映像フレームレート向上
- ブラウザ応答性の改善

---

### パフォーマンス計測情報

**ブラウザコンソール（ユーザー側）:**
```javascript
[performOCR] Capture response received in 150.25ms  // キャプチャ速度
[performOCR] OCR response received in 2456.73ms     // OCR処理時間（改善前: 6-9秒）
[performOCR] Text comparison - current: ...         // テキスト比較結果
```

**サーバーログ（Mac ターミナル）:**
```
[OCR Benchmark] Image resized (1080, 1920) -> (675, 1200), ocr.ocr() took 2.40s
[OCR Benchmark] Image resized (1080, 1920) -> (675, 1200), ocr.ocr() took 2.18s
```

**パフォーマンス改善実績（8月19日）** ✅

- **OCR処理時間の短縮**: 6～9秒 → 1.75～3.39秒（平均2.3秒）
- **改善内容**: 画像リサイズ処理の追加（PaddleOCR処理前に長辺1200pxに制限）
- **効果**: OCRの検出処理が画像サイズに比例するため、解像度を抑えることで大幅な時間短縮を実現

スキャン間隔の確認：
```javascript
[Interval Set] Scanning will repeat every 2 seconds  // スキャン間隔設定値
```

---

### 既知の課題と今後の改善案

**優先度：最高（次回実装）** ⭐

課題改善
- [ ] **通称名（日本語名など）の項目選択改善**
  - 現在: PubChemのsynonymsからランダムに選択される場合がある
  - 改善案: より正確な項目マッチングロジックの実装
  - 予定: 8月20日以降
- [ ] **リストのエクスポート機能**
  - Excelファイル（.xlsx）出力
  - CSV形式出力
  - 予定: 8月20日以降

**優先度：中**

継続的な改善案
- [ ] OCR処理を Web Worker へ移動（メインスレッド非ブロック化）
- [ ] 前フレーム差分チェック（異なる時のみOCR実行）
- [ ] ローカルストレージでのリスト永続化
- [ ] リスト並び替え機能（CAS番号、化合物名でソート）

**優先度：低**

長期改善案
- [ ] WebSocket導入（ポーリングから双方向通信へ）
- [ ] サーバー側OCR処理（クライアント負荷軽減）
- [ ] IndexedDBによるローカルキャッシング
- [ ] 複数言語対応の拡充（日本語、中国語の優先度向上）
- [ ] 試薬瓶の枠内検出機能（フレーム内に来たら自動スキャン開始）

**UI/UX特徴**
- 「リストに追加」ボタン：青色、化合物情報がない場合は非表示
- テーブルヘッダー：薄灰色背景で視認性向上
- 削除ボタン：赤色で注意色として機能
- ホバー効果：行にマウスオーバーでハイライト
- 空状態メッセージ：「リストに試薬を追加してください」

**動作確認テスト結果** ✅

| CAS番号 | 化合物名 | 分子式 | 分子量 | 状態 |
|---------|---------|--------|--------|------|
| 89-83-8 | 5-methyl-2-propan-2-ylphenol | C10H14O | 150.22 | ✅ |
| 58-96-8 | 1-[(2R,3R,4S,5R)-3,4-dihydroxy-5-(hydroxymethyl)oxolan-2-yl]pyrimidine-2,4-dione | C9H12N2O6 | 244.2 | ✅ |

- ✅ リストへの追加機能が正常に動作
- ✅ テーブル表示が正確
- ✅ 削除ボタンが機能
- ✅ 複数試薬の連続追加が可能

---

### Step1-6

Excel出力

完成条件

一覧をExcelへ保存できること。

---

## Step2

既存Excel台帳との互換化

追加予定

- 列順
- 項目
- フォーマット
- 出力様式

---

## Step3

SQLite導入

追加予定

- データ保存
- 編集
- 削除
- 重複チェック

---

## Step4

法令情報追加

追加予定

- SDSリンク
- GHS
- 毒劇物
- PRTR
- リスクアセスメント
- 発がん性

※ 表示のみ実装

---

## Step5

既存試薬台帳との統合

追加予定

- 保管場所
- メーカー
- ロット番号
- 在庫管理
- QRコード

---

# Git運用ルール

## 初期化

```bash
git init
```

---

## 状態確認

```bash
git status
```

---

## 追加

```bash
git add .
```

---

## コミット

```bash
git commit -m "Camera completed"
```

---

## Push

```bash
git push
```

---

# バージョン管理

v0.1.0

FastAPI

---

v0.2.0

Camera

---

v0.3.0

OCR

---

v0.4.0

CAS Validation

---

v0.5.0

PubChem

---

v0.6.0

Compound List

---

v0.7.0

Excel Export

---

v1.0.0

Step1完成

---

# 開発時のルール

毎回

1. 実装
2. 動作確認
3. Git Commit
4. GitHub Push

までを1セットとする。

---

# 実装上の考慮事項

## OCR精度とその制限

### 現状（Step1-2完了時点）

- **PaddleOCR** を使用してカメラ映像からテキストを抽出
- 信頼度の高いテキスト（95%以上）は正確に認識
- 複数言語（英語、日本語、中国語など）が混在するラベルに対応可能

### 課題と今後の対応

1. **CAS番号がない試薬瓶への対応**
   - 直接CAS番号を読み込めない試薬が存在
   - 化合物名や化学式から逆引きしてCAS番号を取得する必要がある
   - **Step1-3で実装予定**：PubChem APIを使用した逆引き機能

2. **OCR誤認識への対応**
   - 低信頼度（<70%）のテキストは除外する検討
   - 複数フレーム平均を取る（改善予定）
   - ユーザーマニュアル入力による確認フロー

3. **言語依存性**
   - 現在は英語（lang='en'）に設定
   - 日本語試薬に対応する場合は `lang='ja'` に変更必要

## PubChem API活用戦略

### Step1-3での実装予定

```
OCR結果（化合物名/化学式）
    ↓
PubChem API検索
    ↓
CAS番号取得
    ↓
化合物情報一覧に追加
```

### 検索優先順位

1. CAS番号による直接検索（最も正確）
2. IUPAC名による検索
3. 化学式による検索
4. 商品名による検索（曖昧性が高い）

---

# 将来実装予定

- SDS検索
- リスクアセスメント対象判定
- GHS表示
- 毒劇物判定
- 発がん性表示
- PRTR
- 消防法
- 在庫管理
- QRコード
- 試薬ラベル印刷

※ これらは Step4以降で実装する。

---

# 最終目標

研究室の試薬管理台帳と完全統合し、

OCRによる登録

↓

法令情報取得

↓

在庫管理

↓

Excel出力

↓

既存台帳との同期

までを一つのWebアプリで実現する。